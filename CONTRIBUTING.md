# 🤝 Руководство по внесению вклада

Спасибо за ваш интерес к улучшению проекта! Мы рады любому вкладу.

## 🌟 Как помочь проекту

### Способы участия

- 🐛 **Сообщайте об ошибках** - создавайте детальные bug reports
- 💡 **Предлагайте идеи** - делитесь идеями новых функций
- 📖 **Улучшайте документацию** - исправляйте опечатки, дополняйте README
- 🔧 **Исправляйте баги** - находите и устраняйте проблемы
- ✨ **Добавляйте функции** - реализуйте новые возможности
- 🌍 **Переводите** - помогите с локализацией

## 📝 Процесс внесения изменений

### 1. Подготовка

```bash
# Форкните репозиторий на GitHub

# Клонируйте ваш форк
git clone https://github.com/ваш-username/TransGoogleTest.git
cd TransGoogleTest

# Добавьте upstream remote
git remote add upstream https://github.com/original/TransGoogleTest.git

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # или venv\Scripts\activate на Windows

# Установите зависимости
pip install -r requirements.txt
```

### 2. Создание ветки

```bash
# Обновите master
git checkout master
git pull upstream master

# Создайте ветку для вашей работы
git checkout -b feature/название-фичи
# или
git checkout -b fix/название-бага
```

Названия веток:
- `feature/новая-функция` - для новых функций
- `fix/исправление-бага` - для исправлений
- `docs/улучшение-документации` - для документации
- `refactor/рефакторинг` - для рефакторинга кода

### 3. Внесение изменений

#### Стиль кода

- Используйте **4 пробела** для отступов (не табы)
- Следуйте **PEP 8** для Python кода
- Добавляйте **docstrings** для функций и классов
- Используйте **type hints** где возможно
- Пишите **понятные комментарии** на русском или английском

Пример хорошего кода:

```python
def translate_text(text: str, source_lang: str, target_lang: str) -> str:
    """
    Переводит текст с одного языка на другой.
    
    Args:
        text: Текст для перевода
        source_lang: Исходный язык (ru, en, etc)
        target_lang: Целевой язык (ru, en, etc)
        
    Returns:
        Переведенный текст или сообщение об ошибке
    """
    if not text.strip():
        return ""
    
    # Логика перевода...
    return translated_text
```

#### Структура проекта

```
TransGoogleTest/
├── app/
│   ├── config.py           # Конфигурация - НЕ хардкодить ключи!
│   ├── models/             # Модели данных
│   ├── services/           # Бизнес-логика
│   ├── ui/                 # Интерфейс
│   └── widgets/            # Переиспользуемые виджеты
├── tests/                  # Тесты (если добавляются)
├── docs/                   # Дополнительная документация
└── main.py                 # Точка входа
```

#### Безопасность

⚠️ **ВАЖНО: Никогда не коммитьте секреты!**

- ❌ НЕ хардкодите API ключи в коде
- ❌ НЕ коммитьте `config.json` с реальными ключами
- ✅ Используйте `config.json.example` для примеров
- ✅ Все ключи должны загружаться из конфига
- ✅ Проверяйте `.gitignore` перед коммитом

### 4. Тестирование

Перед созданием PR убедитесь, что:

- ✅ Приложение запускается без ошибок
- ✅ Новая функция работает как ожидается
- ✅ Не сломаны существующие функции
- ✅ Нет лишних файлов в коммите (проверьте `git status`)
- ✅ Код следует стилю проекта

```bash
# Проверьте что приложение работает
python main.py

# Проверьте импорты
python -c "from app.ui.main_window import GoogleWebSpeechTranslator"
```

### 5. Коммит изменений

```bash
# Добавьте файлы
git add .

# Проверьте что добавили
git status

# Создайте коммит с понятным сообщением
git commit -m "feat: добавлена поддержка испанского языка

- Добавлены испанские голоса в конфиг
- Обновлен словарь языков
- Добавлены тесты для испанского
"
```

#### Формат сообщений коммитов

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```
<тип>: <краткое описание>

<подробное описание>

<footer>
```

**Типы:**
- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование кода (без изменения логики)
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, конфигурации и т.д.

**Примеры:**

```bash
feat: добавлена поддержка Google Cloud TTS
fix: исправлена ошибка при пустом тексте в TTS
docs: обновлена инструкция по установке
refactor: вынесен TTS код в отдельный сервис
```

### 6. Создание Pull Request

```bash
# Отправьте ветку на GitHub
git push origin feature/название-фичи
```

Затем на GitHub:

1. Откройте ваш форк
2. Нажмите "Compare & pull request"
3. Заполните шаблон PR:

```markdown
## 🎯 Описание

Краткое описание изменений.

## 🔄 Тип изменений

- [ ] Исправление бага (fix)
- [ ] Новая функция (feature)
- [ ] Критическое изменение (breaking change)
- [ ] Документация (docs)

## ✅ Чеклист

- [ ] Код следует стилю проекта
- [ ] Приложение запускается без ошибок
- [ ] Обновлена документация (если нужно)
- [ ] Нет хардкоженных секретов
- [ ] Проверено на Windows/macOS/Linux (укажите вашу ОС)

## 🧪 Как протестировать

1. Запустите приложение
2. Перейдите в ...
3. Проверьте что ...

## 📸 Скриншоты (если применимо)

Добавьте скриншоты новой функции.
```

## 🐛 Сообщение об ошибках

Используйте [шаблон Bug Report](.github/ISSUE_TEMPLATE/bug_report.md)

Обязательно укажите:
- Версию Python
- Операционную систему
- Шаги для воспроизведения
- Ожидаемое и фактическое поведение
- Логи ошибок

## 💡 Предложение функций

Используйте [шаблон Feature Request](.github/ISSUE_TEMPLATE/feature_request.md)

Опишите:
- Что вы хотите добавить
- Зачем это нужно
- Как вы видите реализацию
- Возможные альтернативы

## 📚 Документация

Если вы изменили функциональность, обновите:

- `README.md` - основное описание
- `INSTALL.md` - если изменилась установка
- `QUICKSTART.md` - если изменился быстрый старт
- Комментарии в коде
- `config.json.example` - если добавлены новые настройки

## 🎨 UI/UX изменения

При изменении интерфейса:

- Добавьте скриншоты в PR
- Убедитесь что изменения работают на разных разрешениях
- Проверьте что интерфейс интуитивен
- Используйте существующий стиль и цветовую схему

## 🌍 Локализация

Для добавления нового языка:

1. Добавьте язык в `LANGUAGE_MAP` в `app/config.py`
2. Добавьте голос для языка в `TTS_VOICES`
3. Обновите документацию
4. Добавьте примеры в README

## ⚡ Производительность

Если ваши изменения влияют на производительность:

- Объясните почему изменения необходимы
- Предоставьте бенчмарки до/после (если возможно)
- Рассмотрите альтернативные подходы

## ❓ Вопросы

Если у вас есть вопросы:

1. Проверьте [README.md](README.md) и [INSTALL.md](INSTALL.md)
2. Поищите в существующих [Issues](../../issues)
3. Создайте новый Issue с вопросом

## 📞 Связь

- **GitHub Issues** - для багов и предложений
- **Pull Requests** - для кода
- **Discussions** - для общих вопросов (если включено)

## 🙏 Спасибо!

Каждый вклад ценен, будь то исправление опечатки или реализация большой функции. Спасибо что помогаете улучшать проект! ❤️

---

**Happy coding! 🚀**
