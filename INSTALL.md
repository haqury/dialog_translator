# 📚 Пошаговая инструкция по установке

Подробное руководство по установке и настройке приложения для начинающих.

## 📋 Предварительные требования

### Windows

1. **Python 3.8 или новее**
   - Скачайте с [python.org](https://www.python.org/downloads/)
   - При установке обязательно отметьте "Add Python to PATH"
   - Проверьте установку: откройте CMD и введите `python --version`

2. **Git** (для клонирования репозитория)
   - Скачайте с [git-scm.com](https://git-scm.com/)
   - Или скачайте проект как ZIP архив

3. **Микрофон** (для распознавания речи)
   - Встроенный микрофон ноутбука или внешний USB микрофон

### macOS

1. **Python 3.8+**
   ```bash
   brew install python3
   ```

2. **PortAudio** (для sounddevice)
   ```bash
   brew install portaudio
   ```

### Linux (Ubuntu/Debian)

1. **Python 3.8+**
   ```bash
   sudo apt update
   sudo apt install python3 python3-pip
   ```

2. **Зависимости для аудио**
   ```bash
   sudo apt install portaudio19-dev python3-pyaudio
   ```

## 🚀 Установка приложения

### Шаг 1: Получение кода

**Вариант А: Через Git**

```bash
# Клонируйте репозиторий
git clone <url-репозитория>

# Перейдите в папку проекта
cd TransGoogleTest
```

**Вариант Б: Скачать ZIP**

1. Нажмите кнопку "Code" → "Download ZIP" на GitHub
2. Распакуйте архив в удобное место
3. Откройте CMD/Terminal и перейдите в папку проекта

### Шаг 2: Создание виртуального окружения (рекомендуется)

**Windows:**
```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать
venv\Scripts\activate
```

**macOS/Linux:**
```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать
source venv/bin/activate
```

После активации в начале строки CMD/Terminal появится `(venv)`.

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

**Если возникает ошибка с PyAudio на Windows:**

1. Скачайте готовый wheel файл с [www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio](https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio)
2. Выберите версию для вашей Python (например, `PyAudio‑0.2.13‑cp311‑cp311‑win_amd64.whl` для Python 3.11 64-bit)
3. Установите:
   ```bash
   pip install PyAudio‑0.2.13‑cp311‑cp311‑win_amd64.whl
   ```

### Шаг 4: Создание конфигурационного файла

```bash
# Windows
copy config.json.example config.json

# macOS/Linux
cp config.json.example config.json
```

### Шаг 5: Получение API ключа ElevenLabs (для озвучивания)

#### 5.1. Регистрация

1. Откройте [elevenlabs.io](https://elevenlabs.io/)
2. Нажмите "Sign Up" в правом верхнем углу
3. Зарегистрируйтесь через email или Google аккаунт

#### 5.2. Получение API ключа

1. После входа нажмите на ваш профиль (правый верхний угол)
2. Выберите "Profile Settings"
3. Прокрутите вниз до раздела "API Key"
4. Нажмите "Copy" чтобы скопировать ключ

API ключ выглядит примерно так: `sk_abcd1234efgh5678ijkl9012mnop3456`

#### 5.3. Добавление ключа в конфигурацию

Откройте файл `config.json` в текстовом редакторе (Notepad, VS Code, и т.д.) и найдите строку:

```json
"elevenlabs_api_key": "",
```

Вставьте ваш API ключ между кавычками:

```json
"elevenlabs_api_key": "sk_abcd1234efgh5678ijkl9012mnop3456",
```

Сохраните файл.

### Шаг 6: Первый запуск

```bash
python main.py
```

При первом запуске:
- Приложение откроется в полноэкранном режиме
- Нажмите F11 или кнопку "⛶" для выхода из полноэкранного режима
- Откройте настройки (кнопка ⚙️)

## ⚙️ Начальная настройка

### 1. Выбор языков

В панели настроек:
- **Язык 1** - ваш родной язык (например, 🇷🇺 RU)
- **Язык 2** - язык перевода (например, 🇺🇸 EN)

### 2. Выбор микрофона

1. В разделе "Микрофон" выберите ваше устройство записи
2. Говорите в микрофон и наблюдайте за индикатором громкости
3. Если индикатор не реагирует, попробуйте другое устройство

### 3. Настройка чувствительности

Параметр `energy_threshold` определяет, насколько громко нужно говорить:
- **Значение по умолчанию**: 300
- **Тихий микрофон**: уменьшите до 100-200
- **Шумное помещение**: увеличьте до 500-1000

### 4. Настройка TTS (озвучивания)

1. Убедитесь, что `enable_tts` включен
2. Выберите провайдер: `elevenlabs`
3. Выберите голос из списка (по умолчанию Roger для английского)
4. Настройте громкость (0-100) и скорость (0.5-2.0)

## 🎯 Тестирование

### Проверка распознавания речи

1. Нажмите кнопку микрофона 🎤
2. Скажите что-нибудь на выбранном языке
3. Через 1-2 секунды после паузы текст должен появиться в чате

Если не работает:
- Проверьте интернет соединение (нужен Google Web Speech API)
- Говорите четче и громче
- Попробуйте другой микрофон в настройках
- Уменьшите `energy_threshold`

### Проверка озвучивания

1. Нажмите кнопку 🔊 рядом с любым переведенным сообщением
2. Должно прозвучать озвучивание

Если не работает:
- Проверьте, что API ключ правильный в `config.json`
- Убедитесь, что модель `eleven_turbo_v2` (не v1!)
- Проверьте баланс символов на elevenlabs.io
- Выберите правильное устройство воспроизведения в настройках

## 🔧 Решение распространенных проблем

### Ошибка "No module named 'PyQt5'"

```bash
pip install PyQt5
```

### Ошибка "No module named 'speech_recognition'"

```bash
pip install SpeechRecognition
```

### Ошибка при установке PyAudio

**Windows:**
Скачайте wheel файл с [www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio](https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio)

**Linux:**
```bash
sudo apt install portaudio19-dev python3-pyaudio
pip install pyaudio
```

**macOS:**
```bash
brew install portaudio
pip install pyaudio
```

### Микрофон не работает

1. **Проверьте разрешения** - Windows может заблокировать доступ к микрофону
   - Настройки → Конфиденциальность → Микрофон
   - Разрешите настольным приложениям доступ к микрофону

2. **Проверьте устройство в системе**
   - Windows: Параметры звука → Ввод
   - Говорите в микрофон и смотрите на индикатор

3. **Попробуйте другое устройство** в настройках приложения

### Озвучивание очень медленное

1. Используйте модель `eleven_turbo_v2` вместо `eleven_multilingual_v2`
2. Проверьте интернет соединение
3. Уменьшите `tts_speed` в настройках

### Ошибка "API key invalid"

1. Проверьте, что ключ начинается с `sk_`
2. Убедитесь, что нет лишних пробелов в начале/конце
3. Проверьте ключ на [elevenlabs.io/app/settings](https://elevenlabs.io/app/settings)

## 💡 Полезные советы

### Горячие клавиши

- **F11** - полноэкранный режим
- **ESC** - выход из полноэкранного режима
- Двойной клик на заголовке - развернуть/свернуть окно

### Экономия символов ElevenLabs

Бесплатный тариф: 10,000 символов/месяц

- Отключите автовоспроизведение (`auto_play_tts: false`)
- Озвучивайте только нужные сообщения кнопкой 🔊
- Используйте короткие фразы

### Настройка для разных сценариев

**Для презентаций:**
```json
{
    "auto_play_tts": true,
    "tts_volume": 100,
    "opacity": 1.0,
    "font_size": 14
}
```

**Для личного использования:**
```json
{
    "auto_play_tts": false,
    "tts_volume": 60,
    "opacity": 0.9,
    "font_size": 11
}
```

**Для шумных мест:**
```json
{
    "energy_threshold": 800,
    "pause_threshold": 1.2
}
```

## 📞 Получение помощи

Если у вас остались вопросы:

1. Проверьте [README.md](README.md) для общей информации
2. Посмотрите раздел "Устранение неполадок" в README.md
3. Откройте issue на GitHub с описанием проблемы

При создании issue укажите:
- Версию Python (`python --version`)
- Операционную систему
- Текст ошибки (если есть)
- Что вы пытались сделать

## 🎉 Готово!

Теперь вы готовы использовать переводчик! 

Удачи! 🚀
